.env
*.log
.DS_Store
Thumbs.db
*.local

# SQLite transient runtime files
*.db-shm
*.db-wal

# Business KB directories — runtime data, mounted via docker volume.
# Each business gets its own nested git repo at kb/{slug}/.
/kb/

# Agent runtime state (regenerated by agent runs)
server/agents/*/run-log.jsonl
server/agents/*/memory.json
server/agents/*/inbox.jsonl
