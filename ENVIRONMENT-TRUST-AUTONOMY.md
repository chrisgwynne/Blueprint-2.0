# Environment: Trust and Autonomy

No new required environment variables were added. Existing LLM provider settings and BAP registration settings remain in use.

Relevant existing variables:
- `LLM_DEFAULT_PROVIDER`
- provider API keys such as `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `GOOGLE_API_KEY`
- `BAP_REGISTRATION_SECRET`
- server/session/database variables already documented in `.env.example`
